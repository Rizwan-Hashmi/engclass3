import React from 'react';

const Riz2 = ({name, level, title}) => {
    return <div><center> 
    <h1>{title}</h1>
         
    Hello to everyone from <strong>{name}</strong>, Learning level {level}  from file {level+2}
    
  </center>
  </div>

}
export default Riz2
